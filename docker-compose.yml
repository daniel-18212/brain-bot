services:
  brain-bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: brain-bot-service
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
